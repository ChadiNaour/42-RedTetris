import React from "react";
import { StyledStartButton } from "./StyledStartButton";

const StartButoon = () => (
    <StyledStartButton>
        Start game
    </StyledStartButton>
);

export default StartButoon;